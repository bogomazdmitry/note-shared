import { Component, Input, OnInit } from '@angular/core';
import { BaseNotification } from 'src/app/shared/models/base-notification.model';

export interface BaseNotificationComponent {
  notification: any;
}
